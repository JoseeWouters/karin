import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'nl.joseewouters.karin',
  appName: 'karin',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
